# 元组tuple：可以一次存储多个数据但【不能修改】
# 但如果元组中嵌套列表，列表是可以修改的
# 元组名称 = (数据1, 数据2, ...)
# 如果元组中只有一个数据则需要在其后面加逗号，否则就不是元组类型数据了 t1=(10,)

tuple1 = (10,)
tuple2 = (10, 20, 30)
# 查询元组中数据
'''
元组名称[索引下标]
index():查找某数据，返回对顶下标，否则报错
count():查找某数据出现的次数
len():元组中数据的个数
'''
